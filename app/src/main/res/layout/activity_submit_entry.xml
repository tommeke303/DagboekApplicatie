<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="be.hogent.dagboekapplicatie.SubmitEntryActivity">

    <TextView
        android:id="@+id/diary_title"

        android:text="@string/diary_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"/>
    <EditText
        android:id="@+id/editText_diary_title"
        android:background="@drawable/back"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/diary_title"/>
    <TextView
        android:id="@+id/diary_content_title"
        android:text="@string/content_title"
        android:layout_below="@id/editText_diary_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
    <Button
        android:id="@+id/submit_button"
        android:layout_width="match_parent"
        android:text="@string/submit"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"/>
    <EditText
        android:id="@+id/editText_content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/submit_button"
        android:layout_below="@id/diary_content_title"
        android:background="@drawable/back"/>



</RelativeLayout>
